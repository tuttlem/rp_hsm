# Release Exception Template

## Exception Header

- `exception_id`:
- `candidate_id`:
- `status`: `proposed`
- `prepared_by`:
- `prepared_on`:

## Scope

- `affected_check_ids`:
- `candidate_artifact_name`:
- `candidate_artifact_version`:
- `candidate_artifact_hash`:

## Issue Summary

- `issue_summary`:
- `why_default_release_bar_failed`:
- `security_impact`:

## Mitigation and Operator Impact

- `current_mitigation`:
- `operator_caveat`:
- `residual_risk`:

## Approval and Revisit

- `approval_owner`:
- `approval_timestamp`:
- `expiry_or_revisit_trigger`:
- `follow_up_requirement`:

## Non-Waivable Reminder

This template must not be used to waive:

- artifact identity
- release decision traceability
- dependency review entirely
- build review entirely
- visibility of unresolved security impact
